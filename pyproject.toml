[project]
name = "esg-portfolio-backend"
version = "0.1.0"
description = "ESG Portfolio Optimization Chatbot — FastAPI Backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
